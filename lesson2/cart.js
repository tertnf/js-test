let selaries = [4000, 6000, 10000, 20000, 50000];
let totalSalary = 0;
for (let selary of selaries) {
    totalSalary += selary;
 }
// console.log(totalSalary);


selaries.forEach(element => {
    console.log(element);
});
